{
  "name": "3x-app",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "turbo clean",
    "dev:app": "turbo dev --filter=app",
    "dev:admin": "turbo dev --filter=admin",
    "build:app": "turbo build --filter=app",
    "build:admin": "turbo build --filter=admin",
    "seed:super-admin": "node scripts/seed-super-admin.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "pg": "^8.20.0"
  }
}
